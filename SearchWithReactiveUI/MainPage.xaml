<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:SearchWithReactiveUI" 
             x:Class="SearchWithReactiveUI.MainPage"
             Title="Search page">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <SearchBar Grid.Row="0" Placeholder="Search items..." Text="{Binding SearchText}" TextChanged="SearchBar_TextChanged" />
        <ListView Grid.Row="1" ItemsSource="{Binding ItemsDisplayed}" />
    </Grid>
    
</ContentPage>